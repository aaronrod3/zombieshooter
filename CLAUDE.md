# ZombieShooter

UE5.8 co-op zombie survival game (working title). Solo dev, C++ core / Blueprint content. Pivoted 2026-07-18 from FP/TP shooter to Project Zomboid-style top-down survival: needs/moodles, noise-as-threat, permadeath+persistent world, cure arc, 2-4p listen-server co-op.

**Read `Docs/SessionHandoff.md` first, every session — it is kept short on purpose and only covers the last completed task and the immediate next step. Full history lives in git commit log, not in this repo's docs.**

Docs: `Docs/GameDevPlan.md` = design plan of record — pillars, scope contract, decisions (§1-§8). `Docs/Beta/` = production plan to beta — phases B0-B12, start at `Docs/Beta/README.md`. `Docs/Phases/P<N>_*.md` = frozen pre-Beta-plan status stubs, historical only, don't restart them. `Docs/SessionHandoff.md` = current status only (rewritten every session, not appended to).

`Docs/CoreLoopPlan.md` and `Docs/TaskTracker.md` are **retired** — pre-pivot artifacts, historical only, do not reference them for current work.

## Naming (critical)
- `ZS` prefix for all non-zombie classes/assets: `AZSPlayerCharacter`, `AZSGameMode`, `AZSGameState`, `AZSPlayerState`, `AZSPlayerController`, `AZSWeapon`, `UZSHealthComponent`, `UZSWeaponConfig`, `BP_ZS_*`, `DA_ZS_*`, `WBP_ZS_*`.
- "Zombie" reserved only for enemy: `AZombieCharacter`, `AZombieAIController`, `BP_Zombie_*`. Never mix — player must never be confused with enemy.
- Input Actions unprefixed (`IA_Fire`); Input Mapping Contexts prefixed (`IMC_ZS_Default`).

## Commands
- Build (Editor): Rider/VS → `ZombieShooterEditor Win64 Development`, or `Build.bat ZombieShooterEditor Win64 Development -project=...uproject -waitmutex`
- Gen project files: `Build.bat -projectfiles -project=...uproject -game -engine`
- Test: PIE only, no automated suite. Multiplayer: PIE Multiplayer Options (Players ≥2, listen-server).
- Header change → regen project files if needed. Live Coding (Ctrl+Alt+F11) OK for .cpp-only.
- Git + LFS, branch `main`. Commit after each sub-task. Never force-push main.

## Architecture
- **Framework/**: `AZSGameMode` (server rules), `AZSGameState` (replicated world clock — `TimeOfDayHours`/`DayCount`/utilities-shutoff timer — plus sleep/time-skip readiness aggregation across `PlayerArray`, and the finite per-session rarity loot pool used by `Inventory/`), `AZSPlayerState` (health/ammo/kills, unpopulated), `AZSPlayerController` (Enhanced Input via `IMC_ZS_Default`+`IMC_ZS_MouseLook`, wired via `ConstructorHelpers::FObjectFinder`, not BP CDO).
- **Player/**: `AZSPlayerCharacter` — TP/TopDown dual perspective. `TopDown` is the default camera (fixed pitch/yaw); `ToggleCameraPerspective`/`IA_ToggleView` (`V`) swaps to `ThirdPerson` as an "OverShoulder" fallback. Hybrid cursor facing (`UpdateCursorFacing`) overrides actor rotation toward the mouse cursor's ground-plane projection, gated to aiming/attacking/interacting only. Move/look/jump/crouch/sprint/aim/fire/reload/melee are all server-RPC+OnRep replicated. Weapon equip is config-driven through a fixed 9-slot `HotbarSlots` array (`CurrentWeapon` **is** the active slot — deliberately not renamed to avoid a Blueprint-reference-corruption risk, see the Live Coding lesson below); `SelectHotbarSlot`/`CycleHotbar` route through `Server_SelectHotbarSlot`, which schedules the actual equip after `EquipTimeSeconds`/`UnequipTimeSeconds`, gated by `bIsBusy`. `IA_Attack` dispatches on `CurrentWeapon->GetConfig()->AttackType` (`EZSAttackType`, `ZSWeaponTypes.h`) — `Ranged` into hitscan fire (`Server_Fire`, traces from `SocketMuzzle`), `Melee`/no-weapon into `PerformMeleeSwing` (shared bare-fist/weapon-melee logic). `Player/Animation/UZSAnimInstanceBase` = native TP AnimBP parent. **Not yet built**: `SecondaryHand` (action-trigger mechanism still an open design question, `GameDevPlan.md` §7), shove/stomp, stamina cost on melee.
- **Interaction/**: `UZSInteractableComponent` — attach to any actor to make it interactable; `AZSPlayerCharacter::TryInteract`/`Server_Interact` is the server-authoritative entry point, `UpdateNearestInteractable` does a per-tick sphere-overlap scan against `ECC_WorldStatic`/`ECC_WorldDynamic`. No world-prompt widget yet — `OnNearestInteractableChanged` is the C++ hook for one.
- **Survival/**: `UZSNeedsComponent` — replicated Hunger/Thirst/Fatigue/Stamina, rates/curves from `UZSNeedsConfig`. Hunger/Thirst/Fatigue decay on `AZSGameState`'s game-hour clock; Stamina is real-time (drains sprinting, regens idle), gates `StartSprint`. `GetPerformanceMultiplier()` feeds healing rate/aim accuracy/attack recovery. `UZSItemConfig` is the eat/drink/use data contract, consumed via `Server_ConsumeItem`/`Server_UseItem`. Sleep/time-skip: `AZSPlayerCharacter::RequestSleep`/`CancelSleepReady` (per-player ready flag) + `AZSGameState::UpdateSleepRequestState` (all-players-ready aggregation + clock advance); `IsSafeToSleep()` is stubbed `true`. No moodle UI yet — `OnHungerChanged`/etc. are the hooks.
- **Weapons/**: `AZSWeapon` (replicated config/fire-mode/ammo; root component `BaseWeaponMesh`, a `UStaticMeshComponent` — no skeletal weapon meshes anywhere), `UZSWeaponConfig` (the full per-weapon data contract: Setup meshes (`BaseWeaponMesh`/`TriggerMesh`/`MagazineMesh`/`TP_Mesh`), Attachments (`MuzzleMesh`/`HandguardMesh`/`GripMesh`/`OpticMesh`, each with its own `SocketX` field), fire/melee/durability/knockback tunables, and `AttackType`), `AZSMagazine` (cosmetic prop, also static-mesh-based), notify classes `AN_ZS_UnlockActions`/`ANS_ZS_BlockADS`/`ANS_ZS_LeftHandGrip`.
- **Combat/**: `UZSHealthComponent` — replicated `CurrentHealth` + 4 `FZSBodyZoneWound` (Head/Torso/Arms/Legs), each with a wound type (Scratch/Laceration/Fracture/Bite) and bleed/dirty/splinted/amputated flags. All damage flows through `AZSPlayerCharacter::TakeDamage` (zone inferred from hit bone name, wound type from the damage type class via `ZSDamageTypes.h`'s marker classes) into `Server_ApplyDamage` — nothing else mutates health directly, per the replication convention below. Bite wounds roll a hidden infection chance; `EZSInfectionStage` progresses on `AZSGameState`'s game-hour clock. `Server_AmputateZone` clears an active infection at the cost of a permanent zone penalty (any zone, solo-capable, no tool-item gate). `GetMobilityMultiplier()`/`GetAttackSpeedMultiplier()`/`GetReloadSpeedMultiplier()` feed move speed and fire-rate timing. Death → `HandleDeath` → delayed `Destroy()` + `RestartPlayer` respawn. Treatment (`Server_ApplyBandage`/`Server_Disinfect`/`Server_Splint`) and item consumption both route through `Server_UseItem`, dispatching on `UZSItemConfig::EZSItemUseType`.
- **Zombies/**: `AZombieCharacter` — deliberately does not reuse `UZSHealthComponent` (that machinery is player-specific); just a flat replicated `CurrentHealth` seeded from `UZSZombieConfig::MaxHealth`, same `TakeDamage`-only-entry-point rule. `AZombieAIController` wires `AIPerception` (sight+hearing, radii from `UZSZombieConfig` at `OnPossess`) and runs `BT_Zombie`/`BB_Zombie` (`/Game/ZS/Enemy/AI/`) — `HandleTargetPerceptionUpdated` writes Blackboard keys and calls `SetChasing`. `Zombies/AI/` holds all 6 BT task nodes as native C++ (`BTTask_MeleeAttack`/`StartInvestigationTimer`/`StartIdleDwell`/`Wander`/`GetInvestigationPoint`/`ClearLastKnownLocation`, sharing key names via `ZSZombieBlackboardKeys.h`) — no content-Blueprint BT task wrappers. Verified working in PIE (wander/investigate/chase). `UZSZombieConfig` covers speed/health/senses/damage/mesh/anim-class/BT per zombie type — a second type is a new config instance, never a C++ branch, per the multi-config rule below. `UZSNoiseSystem::ReportNoise` (wraps `UAISense_Hearing::ReportNoiseEvent`) is wired into weapon fire (`FireNoiseRadius`) and sprint start.
- **Inventory/**: `UZSInventoryComponent` — replicated flat `CarrySlots` (stacking, `UZSItemConfig::MaxStackSize`) + two equip slots (`EZSEquipSlot::Back`/`Hip`) granting `CarryCapacityBonus`. `GetEncumbranceMultiplier()` folds into move speed alongside `Combat/`'s mobility multiplier — a soft penalty past capacity, not a hard carry block. `AZSWorldItemActor` is a replicated pickup (reuses `Interaction/`'s `UZSInteractableComponent`) — `Server_DropItem` spawns one; note its `Item`/`Count` are `VisibleAnywhere` (code-set only via `InitializeItem`, not Details-panel-authorable — a Blueprint subclass calling `InitializeItem` in `BeginPlay` is the pattern for hand-placed pre-populated pickups). `AZSContainerActor` (same interactable reuse) seeds `ContainerSlots` from a `UZSLootTableConfig` at `BeginPlay` and does a "loot all" transfer on interact; its `LootTable` field is `EditDefaultsOnly`, so per-archetype containers need a Blueprint subclass (e.g. `BP_ZS_Container_Kitchen`), not a raw placed `AZSContainerActor`. Rare/VeryRare rolls are gated through a finite per-session pool on `AZSGameState`. **Known gap**: `HotbarSlots` still holds direct `UZSWeaponConfig*` references, not yet backed by items actually carried in this component — the item-instance refactor (`Docs/Planning/InventoryLoadoutEquipping_Plan.md`, tracked as OQ-B0-13) is the planned fix.
- **Replication convention (mandatory)**: `UPROPERTY(ReplicatedUsing=OnRep_X)` + `Server_X()` gated by `HasAuthority()` + `GetLifetimeReplicatedProps()`/`DOREPLIFETIME()` + `OnRep_X()` broadcasts a delegate. Never poll replicated state directly.
- **Multi-config rule**: every weapon or zombie system must generalize to N types via new config data-asset instances, never new C++ branches.
- **Cosmetic attachments must be `NoCollision`**: any mesh rigidly attached to the character (weapon meshes, future held items/clothing) needs collision explicitly disabled. Left at a default blocking profile, it overlaps the owning character's own capsule every tick and `CharacterMovementComponent`'s penetration-resolution logic fights that overlap indefinitely — visible as the character climbing or sliding uncontrollably the instant the mesh attaches. `AZSWeapon::AssignNewStaticMesh` already does this for attachment sub-meshes; anything built directly in a constructor (like `BaseWeaponMesh` itself) needs the same explicit `SetCollisionEnabled(ECollisionEnabled::NoCollision)` call.

## Character Skeleton & Animation
`SKEL_TFA_Mannequin` (Infima's skeleton) is the one shared retarget hub for every humanoid in the project — nothing moves to a different skeleton. Locomotion (idle/walk/run/crouch-walk) comes from **Lyra blend spaces retargeted onto `SKEL_TFA_Mannequin`** (curated into `Content/Animation/ZSAnims/` — Infima's own bundled set has no equivalent directional walk-cycle assets, only static poses), composited via **Layered Blend Per Bone** with Infima's rifle idle/aim poses and montages (fire/reload) for the upper body. A separate raw Lyra/ShooterGame import in `/Game/Animation/` (referencing a never-migrated `SK_Mannequin`) is dead and unused — don't confuse it with the curated `Content/Animation/ZSAnims/` assets.

`ABP_ZS_ThirdPerson`'s AnimGraph: one `BlendListByBool` picks base locomotion (standing vs. crouching) by `bIsCrouched`; a second picks the rifle idle/aim upper-body pose by `bIsAiming`; a `LayeredBoneBlend` layers that pose over locomotion; a third `BlendListByBool` (gated on `UZSAnimInstanceBase::bHasWeaponEquipped`) controls whether the rifle layer applies at all — unarmed shows pure base locomotion with no rifle layer. See the MCP tooling lessons below for the AnimGraph editing workflow and a `BlendListByBool` indexing gotcha.

Scope rule: animation only earns inclusion if readable at gameplay cam distance or gates gameplay timing. Full authorized list + architecture detail: `Docs/GameDevPlan.md` §5.1.

## Conventions
- Epic naming standard (`AMyActor`, `UMyComponent`, `FMyStruct`, `EMyEnum`, `IMyInterface`) + ZS/Zombie rule.
- Booleans: `b` prefix. Pointers: `TObjectPtr<T>` in UPROPERTY, raw `T*` locally.
- `GENERATED_BODY()` on all reflected classes.
- Server mutators prefixed `Server_`. No magic numbers — tunables in `UZSWeaponConfig`/EditAnywhere. Damage only via `TakeDamage()`/`ApplyDamage`. No commented-out code — use branches. Check `Build.cs` before adding heavy modules.
- **Tech split**: C++ = base classes/data contracts/perf-sensitive/shared machinery. Blueprint = gameplay config/tuning. Player/weapon action functions are `BlueprintNativeEvent` so BP children can override without recompiling. AnimGraphs/data assets/BT stay Blueprint.

## Workflow Efficiency (lessons, keep this section short)
- **Don't auto-attempt `Build.bat` while the editor's likely open.** It fails on the Live Coding lock nearly every time (see below) — a wasted round-trip for zero information. State that a rebuild is needed and let the dev trigger it (Rider/Ctrl+Alt+F11), rather than spending a call finding out again.
- **Live Coding (Ctrl+Alt+F11) can silently corrupt Blueprint references to native classes, project-wide, on any patch — broader than just "the class that changed."** Two confirmed incidents, 2026-07-21, same session: (1) after a patch added new `UPROPERTY`s to `AZSPlayerCharacter`, `BP_ZS_PlayerCharacter`'s parent-class link broke (`BlueprintGeneratedClass BP_ZS_PlayerCharacter_C is not a child class of Class ZSPlayerCharacter`), so `AZSGameMode`'s `ConstructorHelpers::FClassFinder` silently fell back to the raw C++ class (wrong pawn, no BP CDO defaults, e.g. no mesh). (2) Separately, three unrelated Blueprints (`BTTask_MeleeAttack`/`StartInvestigationTimer`/`StartIdleDwell`) each had a `Cast To ZombieAIController` node break (`this cast has an invalid target type - was the class deleted without a redirect?`) even though `AZombieAIController`'s header hadn't changed that round at all - the corruption surfaced only once those Blueprints were first *loaded* post-patch (triggered here by an unrelated `unreal-mcp` property read), not at patch time itself. **The real rule: Live Coding reinstances every class in the module on every patch, so any Blueprint anywhere with a cast/reference to a native class in that module is at risk, whether or not that specific class was touched.** No crash, no visible editor error in either case - just silently wrong behavior. Diagnosed both via `EditorToolset.LogsToolset.GetLogEntries`/`BlueprintTools.compile_blueprint`, not guessing. **When a dev reports something that "should just work" behaving wrong after a Live Coding recompile, check the Output Log for `is not a child class of` or `invalid target type` before anything else** (or proactively run `BlueprintTools.compile_blueprint` on any Blueprint about to matter for a test). Reopening/recompiling a broken Blueprint parent-link self-heals; a broken Cast node needs the node itself deleted and recreated (compiling alone doesn't fix it - confirmed via `compile_blueprint` still failing with the stale node in place). Prefer a full editor Compile or `Build.bat` over Ctrl+Alt+F11 whenever practical, and after any Live Coding patch, consider a "Compile All Blueprints" pass (Content Browser bulk action) before trusting PIE results.
- **Check for a running editor with PowerShell's `Get-Process`, not Bash's `tasklist //FI`** — confirmed 2026-07-21 that a `tasklist` image-name filter run through the Bash tool silently matched nothing (wrong quoting through the POSIX-shell layer) while `UnrealEditor.exe` was, in fact, running; `Get-Process | Where-Object ProcessName -match "Unreal|LiveCoding"` caught it correctly. Also: **invoke `Build.bat` via the PowerShell tool, not Bash** — Bash's quoting mangles the batch file's internal `%~dp0`-style calls on this machine (`'C:\Program' is not recognized...`) even when the outer command looks correctly quoted; PowerShell runs it cleanly.
- **`Docs/SessionHandoff.md` is the sole owner of verification status** ("compiled?", "PIE-tested?", "what's next"). `CLAUDE.md`'s Architecture section and the `Docs/Phases/P<N>_*.md` files describe what exists / checklist state — don't restate compile/test status in more than one place, or every status change becomes a 3-file edit.
- **Use targeted reads for large, mostly-static docs** (`GameDevPlan.md`, long class headers) — `Grep` with context or `Read` with offset/limit — instead of reading the whole file when only one section is relevant.
- **Keep TaskCreate/TaskUpdate granularity coarse** — one task per major deliverable/system, not per file. Fine-grained tasks multiply tool-call round-trips without adding real visibility for a solo-dev session.

## MCP / Editor Tooling (lessons, keep this section short)
- `.mcp.json` only proposes the `unreal-mcp` server; actual enablement lives in `.claude/settings.local.json` (gitignored) — won't exist on a new machine until recreated.
- `describe_toolset` on a large toolset (e.g. `BlueprintTools`) can return 70K+ characters — don't call it broadly. Prefer calling a tool blind with a guessed schema first; the error response echoes the exact required schema, which is cheaper than a full describe dump.
- Simulated PIE input (keypresses, viewport clicks) via MCP does **not** reliably reach the pawn — confirmed 2026-07-20 (a simulated `W` never moved `CharMoveComp.Velocity`). Don't spend calls trying to force real gameplay-input verification headlessly; that needs the dev's hands, same as every past multiplayer/input check in this project.
- Poking a replicated/movement-authoritative property directly via `ObjectTools.set_properties` (e.g. `ACharacter::bIsCrouched`) can silently revert next tick if the owning component re-derives it every frame (e.g. from `bWantsToCrouch`) — a "successful" write isn't proof the state actually changed.
- **AnimGraph editing**: `read_graph_dsl`/`write_graph_dsl` return empty for AnimGraph-specific node types (`AnimGraphNode_*`) — use `find_nodes`/`get_node_infos`/`create_node`/`connect_pins` instead, which do support them. Normal EventGraphs (Blueprint logic, not AnimGraphs) work fine with `read_graph_dsl`/`write_graph_dsl`.
- **`FAnimNode_BlendListByBool`'s branch indexing is inverted from the naive bool-to-int mapping**: `bActiveValue == true` selects `BlendPose_0` (index 0), `false` selects `BlendPose_1` (index 1). `create_node`/`connect_pins` don't validate node *semantics*, only pin *connections* — when wiring a new `BlendListByBool`, verify the true/false assignment against a known-working sibling node's wiring, not against intuition.
- **`SkeletalMeshTools.add_socket`'s `bone_name` argument silently fails for at least some bones** (confirmed for a real, non-virtual bone on this project's skeleton — `weapon_r`, verified via the Skeleton Tree UI) — the new socket ends up parented to `root` instead, with only an oblique `SetSocketParent... bone named 'None'` log line as a clue, no thrown error. Works fine for other bones (e.g. `pelvis`). **Workaround**: drive the real Skeleton Tree UI via `SlateInspectorToolset` instead — right-click the target bone → "Add Socket" → rename via F2 — then verify with `get_socket_bone` (that read-only query tool is reliable; it's specifically scripted socket *creation* that's broken).

## Off-Limits
- Don't read/parse `Content/**/*.uasset` as raw binary — use Editor/MCP.
- `Content/InfimaGames/` gitignored, never commit (licensed content) — reinstall via Fab window on fresh clone.
- Don't reference `ShooterGame` project for design decisions (only intentional link: this file was seeded from its structure once).
- Scope is contract: `Docs/GameDevPlan.md` §3 KEEP/SIMPLIFY/REPLACE/CUT table governs what exists. CUT/deferred items (vehicles, full NPC factions, deep crafting, sandbox sliders, seasons) need dedicated planning first.
- No dedicated-server packaging or online subsystem yet — listen-server/direct-IP only.

## Development Order
`Docs/GameDevPlan.md` §4's P0-P10 phase narrative is now historical — outcomes are captured in `Docs/Beta/01_RevisionRegister_P0-P6.md`, and `Docs/Phases/P<N>_*.md` are frozen status stubs, not live checklists. **`Docs/Beta/` is the live production plan** (phases B0-B12, PIE-verified exit criteria per phase) — start at `Docs/Beta/README.md`, current phase and next step live in `Docs/SessionHandoff.md`. Zombie AI uses classic Behavior Trees + Blackboard (not StateTree — standing decision, reconsider later).

## GitHub Workflow
Repo: github.com/aaronrod3/zombieshooter — public (since 2026-07-12, for secret scanning). LFS budget/Actions spend cap = $0 (fail-safe). Branch protection available but not enabled. Never force-push main.
`gh` CLI installed, authenticated as `aaronrod3`. On Windows use full path `/c/Program Files/GitHub CLI/gh.exe` if not on PATH.
Secret scanning requires public repo for personal accounts (private needs GitHub Enterprise) — this is why repo is public; now enabled along with push protection.
Labels `phase-0`–`phase-6` + Projects board ("ZombieShooter Core Loop") set up.

## Reference Docs
- `Docs/Beta/README.md` — production plan to beta, start here for what's next (phases B0-B12, open questions, dependency map).
- `Docs/GameDevPlan.md` — design plan of record: pillars, scope contract §3, skill system §3.1, asset strategy §5/§5.1, decisions §1/§6.
- `Docs/SessionHandoff.md` — current status only, rewritten each session.
- `Docs/ProjectZomboid_DesignReference.md` + `Docs/DevMarkupNotes.md` — PZ systems breakdown + dev markup this project's design was built against.
- `Docs/TuningReference.md` — gameplay tunables, update when new tunables added.
- `Docs/Testing/` — verification runbooks for completed systems.
- `Docs/Infima Pack - Official Implementation Guide/` — Infima's own docs; still actively relevant (skeleton/animation source of record, not a placeholder).
- UE5.8 docs: https://dev.epicgames.com/documentation/unreal-engine/unreal-engine-5-8-documentation — check here first on engine-level setup/config problems (navigation, physics, editor tooling behavior), not just project code.
