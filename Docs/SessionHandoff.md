# Session Handoff

> Read this first, every session. This file is rewritten every session, not appended to — it holds only the last completed task and what's needed next. This is the **sole owner of verification status** (compiled? PIE-tested?) — `CLAUDE.md` describes architecture/current-state design, not status, to avoid the same fact needing edits in multiple places. Full history lives in git commit log, not here.
>
> **Plan of record has two halves.** `Docs/GameDevPlan.md` = design (pillars, §3 scope contract, Decisions). `Docs/Beta/` = production plan to beta (phases B0–B12, tasks, gates). Start at `Docs/Beta/README.md`. Conventions: `CLAUDE.md`.
>
> **Away session?** Read `Docs/AsyncSessionProtocol.md` once now and follow it for the rest of the session without re-reading it.

## Current phase: B1 — UI/UX Foundation, HUD & Input Modes

**B0 → B1 transition confirmed by dev, 2026-07-30.** Two companion docs for B1: `Docs/Beta/B1_UI_UX.md` (task breakdown, entry/exit criteria) and `Docs/Planning/B1_UIDesignSession_2026-07-30.md` (the actual UI design — HUD philosophy, Tab-menu structure, inventory compartments — decided ahead of implementation, read this before touching any layout work).

## B1 progress — 2026-08-01

**Every B1 task group (T1-T8) has its C++ groundwork done, compiled, and automation-tested.** T1/T2/T5.0 closed out earlier this session; T3/T5 landed (`e064bc3`); T6/T7/T8 followed (`00d3633`). **Then, while widget-building was already underway, the dev revised the HUD/hotbar design** (`6d413ac`) - documented below since it changes what several T3/T5 widgets actually are.

**HUD redesign, 2026-08-01 (dev-confirmed, commit `6d413ac`):**
- **No moodle stack on the HUD** - relocated to T5's Loadout panel (`WBP_ZS_MoodleStack` is now a Needs-bars widget inside the Inventory screen, not a HUD element). Matches the design session's "Needs live in the Loadout panel" note taken literally.
- **No 9-slot hotbar grid.** Replaced with a single equipped-item icon (bottom-right). Weapons are key-mapped directly to the 3 mount slots - **1 = Primary, 2 = Pistol, 3 = Secondary** - so mounting a weapon is now the *only* "assignment" step; the old free-form `HotbarSlots` array and its `Server_AssignHotbarSlot`/`Server_ClearHotbarSlot`/`CanAssignToHotbarSlot` API (all built last session) are removed entirely. `AZSPlayerCharacter::ResolveWeaponSlotInstance(SlotIndex)` resolves each key live from `UZSInventoryComponent`'s mount accessors instead.
- **New Equipment slot** (4th key, **G**) for grenades/quick-use equipment - `EquipmentSlotInstanceId`/`Server_AssignEquipmentSlot`/`Server_ClearEquipmentSlot`/`OnEquipmentSlotChanged`, mirrors `SecondaryHandInstanceId`'s pattern. Scoped to `UZSWeaponConfig` instances only this pass (flagged in code + `B1_UI_UX.md` - a non-weapon "other equipment" item isn't dispatchable through it yet, since the equip path only knows how to equip a weapon config).
- **No ammo counter, no scoreboard.** Cut outright, not deferred - the underlying C++ (`OnMagazineAmmoChanged`, `OnPlayerListChanged`/`PlayerListVersion`) stays in place unused, in case either is wanted later.
- All the proven equip-timing machinery (`Server_SelectHotbarSlot`/`CompleteHotbarSwitch`/`bIsBusy` gating/durability writeback) is reused unchanged, just re-pointed at the new resolver - nothing about *how* equipping works changed, only *what* selects what.
- **Required manual step, not yet done**: create `IA_EquipItem` (G key) - the C++ finder is already in place, graceful-if-missing like every other Input Action here.
- 2 new automation tests (`ZS.Loadout.WeaponKeySlotsResolveFromMounts`, `ZS.Loadout.EquipmentSlotRequiresWeaponConfig`) replace the removed hotbar-assignment test. Full rebuild clean, `ZS.` suite (32 tests) clean - only the same 5 pre-existing failures remain.

**Further HUD redesign, 2026-08-02 (dev-confirmed, docs-only — no C++ needed):**
- **T3.2 (health/wound display) and T3.3 (infection indicator) merged into one widget**, `WBP_ZS_BodyConditionIndicator`, Collapsed entirely by default. It only shows a zone's icon when that zone's state actually changes a gameplay multiplier — bleeding, a fracture, amputation, or an infected wound — not for a cosmetic scratch with no mechanical effect. Bite-infection stage is folded into the same widget rather than staying a separate element.
- **No raw numeric health bar anywhere on the HUD anymore.** Nothing elsewhere (T5's Inventory screen never had one either) replaces it — flagged as an open question in `B1_UI_UX.md`'s T3 manual-setup-steps section, not resolved here.
- Needed **zero new C++** — every field the merged widget reads (`bBleeding`/`WoundType`/`bAmputated`/`bCriticalBleed`/`WoundInfectionState` via `Get Zone Wound`, `OnInfectionStageChanged`) was already exposed for the two separate widgets this replaces.
- Manifest artifact re-synced same pass — one `WBP_ZS_BodyConditionIndicator` card replaces the old `WBP_ZS_HealthDisplay`/`WBP_ZS_InfectionIndicator` cards, HUD wireframe down to 4 regions.
- **Manifest artifact rewritten again, 2026-08-02, for detail level (dev feedback: too abstract to follow while actually building)** — every card's Steps now spell out the literal panel/menu/button to click and the exact node-search text to type, instead of pointing back to a shared "Before You Start §B" example. New small helper widget added along the way: `WBP_ZS_StatPreviewLine` (Common/), since Blueprint can't construct a bare Horizontal Box at runtime — only a full Widget Blueprint — so the tooltip's per-line rows needed their own tiny child widget.

**New combat tuning, 2026-08-02, dev-confirmed — "getting hit is a big deal" philosophy: [uncompiled], editor was open this pass, needs your rebuild:**
- New `UZSHealthComponent::GetAccuracySpreadMultiplier()` — a wounded/amputated **Arms** zone now widens weapon spread, not just attack/reload speed. Unlike every other `Get*Multiplier` accessor (1 = full, lower = worse), this one is 1 = no penalty and **higher** is worse, since it multiplies a spread *angle* rather than dividing a speed/rate — flagged clearly in code comments to avoid the inversion tripping up future changes. New config fields `UZSHealthConfig::ArmWoundedAccuracySpreadMultiplier` (1.4) / `ArmAmputatedAccuracySpreadMultiplier` (1.8, harsher, same reasoning as `AmputatedZoneMultiplier`).
- Wired into `AZSPlayerCharacter::FireWeapon` — `SpreadDegrees` is now `BaseSpreadDegrees * GetAccuracySpreadMultiplier()` before the `VRandCone` roll. Melee wasn't touched (no spread-cone concept there).
- New test `ZS.Health.AccuracySpreadMultiplierScalesWithArmWound` — checks the unwounded/wounded/amputated values *and* explicitly checks the penalty direction (`> 1.f`), given how easy the inverted convention would be to get backwards.
- **This was scoped narrowly to what was asked** (the arm-wound accuracy gap flagged while confirming the new Body Condition Indicator's "arms → aim/accuracy, legs → speed" framing) — the broader "player health simplified, can't tank hits" philosophy the dev stated alongside this is **not** implemented; no `UZSHealthConfig::MaxHealth`/zombie melee-damage numbers were touched. That's a separate tuning pass, not guessed at here.
- **Needs a full `Build.bat` rebuild, not Live Coding** — two headers changed (`ZSHealthComponent.h`, `ZSHealthConfig.h`) plus a new file under `Tests/`, both call out for a full rebuild per this doc's own tooling-gotchas section. Editor was running when this was written, so no build/test was attempted this pass (per this project's standing rule not to fight the Live Coding lock).

**Manifest widget-logic moved into C++, 2026-08-02, dev-requested — [uncompiled], same rebuild as the accuracy-multiplier change below:**
- New `UZSUserWidgetBase` convenience getters — `GetOwningZSPlayerCharacter`/`GetOwningHealthComponent`/`GetOwningNeedsComponent`/`GetOwningInventoryComponent` (one node instead of Get Owning Player→Get Controlled Pawn→Cast, which every B1 widget's Graph tab used to hand-wire) and `GetUIManager`/`GetNotificationSubsystem` (same idea for the two local-player subsystems, via `GetOwningLocalPlayer()->GetSubsystem<T>()`). Applies across essentially every card in the manifest.
- New `UZSHealthComponent::GetWoundDisplayCondition(const FZSBodyZoneWound&)` + `HasAnyGameplayAffectingCondition()`, backing a new `EZSWoundDisplayCondition` enum (None/Wounded/Bleeding/Fracture/Infected/Amputated, worst-wins priority) — collapses `WBP_ZS_BodyConditionIndicator`'s hand-wired OR-chain-of-comparisons into one function call + one Switch node per zone. **Caught a real bug while writing this**: the manifest's original Blueprint-described logic only treated `Fracture` as gameplay-affecting, but `GetMobilityMultiplier`/`GetAttackSpeedMultiplier`/`GetReloadSpeedMultiplier` actually penalize Arms/Legs for *any* non-None `WoundType`, not just Fracture — `GetWoundDisplayCondition` is zone-aware and correct now, covered by new test `ZS.Health.WoundDisplayConditionIsZoneAwareAndPrioritized`.
- New `AZSPlayerCharacter::GetKeyLabelForHotbarIndex(int32)` — the "1/2/3/G" key-label mapping in one place instead of also being re-typed as a Blueprint Switch in `WBP_ZS_EquippedItemIndicator`.
- New `UZSDragDropPayload::Make(FGuid, EZSDragSourceKind)` static factory — one node instead of Construct Object from Class + 2 Set nodes in `WBP_ZS_ItemSlot`'s On Drag Detected. **Real build error hit and fixed, 2026-08-02**: the original params were named `InstanceId`/`SourceKind`, shadowing the class's own same-named `UPROPERTY`s - UHT rejects this outright (`cannot be defined in 'Make' as it is already defined in scope... shadowing is not allowed`). Renamed to `NewInstanceId`/`NewSourceKind` (so the Blueprint pins now read "New Instance Id"/"New Source Kind" - manifest updated to match).
- Manifest rewritten a third time to use all of the above — every affected card's Steps now call the new one-node helpers instead of hand-wiring the old chains. `Before You Start §B` keeps the old 3-node chain as a "how it works" explanation but flags that cards below skip it.

**Trial → confirmed, then extended to the whole manifest: every B1 widget's Graph tab moved into C++, 2026-08-02, dev-requested — [uncompiled]:**
- Started as a trial on `WBP_ZS_BodyConditionIndicator` only (new `UZSBodyConditionIndicatorWidget`, `UI/ZSBodyConditionIndicatorWidget.h/.cpp`) — `BindWidget` auto-binds Designer-tab widgets by exact name, `BindWidgetAnimOptional` for the optional `CriticalBleedFlash` pulse, `NativeConstruct()` does the full delegate binding + per-zone refresh natively. Two real errors hit and fixed while building this one: a `BindWidgetAnim` (required, not optional — fixed to `BindWidgetAnimOptional`, confirmed against `Widget.h`'s `EPropertyMetadataKeys` comments) and a UHT param-shadowing error on `UZSDragDropPayload::Make` (`InstanceId`/`SourceKind` params collided with same-named `UPROPERTY`s — renamed to `NewInstanceId`/`NewSourceKind`, commit `49bc865`).
- **Dev confirmed the trial was a real win and asked for it project-wide.** Every remaining B1 Widget Blueprint now has a matching `UZSUserWidgetBase` subclass in `Source/ZombieShooter/UI/` — ~23 new `.h`/`.cpp` pairs covering HUD (`StatPreviewLine`/`StatPreviewTooltip`/`EquippedItemIndicator`/`InteractionPrompt`/`ToastEntry`/`ToastList`), Inventory (`ItemSlot`/`CompartmentPanel`/`WeightBar`/`EquipSlot`/`WeaponMountSlot`/`EquipmentSlot`/`MoodleEntry`/`MoodleStack`/the root `InventoryScreen`), Container (`ContainerLoot`), Death/Sleep (`DeathScreen`/`BlackoutOverlay`/`SleepPlayerRow`/`SleepPrompt`), and MainMenu (`MainMenu`/`PauseMenu`/`LoadingScreen`). `WBP_ZS_Settings` is the one widget deliberately left Blueprint-only (static placeholder, nothing to bind). Every card's **Bind** row in the manifest now says "nothing to bind" and lists what the C++ already does; remaining manual work per card is Designer-tab hierarchy (exact `BindWidget` names) + Class Defaults only.
- New shared getters on `UZSUserWidgetBase`: `GetOwningZSGameState()`/`GetOwningZSGameInstance()`, plus protected `PushAsModal(FName)`/`PopAsModal(FName)` wrapping `UZSUIManager::PushModal`/`PopModal` — every modal-root widget (`Inventory`, `ContainerLoot`, `PauseMenu`, `SleepPrompt`) exposes its own `OpenAsModal()`/`CloseAsModal()` built on top of these.
- **Real bug found and fixed while wiring the equip/mount drop targets**: `UZSInventoryComponent::Server_EquipToSlot`/`Server_MountLongGun`/`Server_MountSidearm` are plain `HasAuthority()`-gated functions, not real RPCs — calling them straight from a client-owned widget (as earlier revisions of this doc's own instructions said to do) silently no-ops on any non-host client. Fixed with new `Server, Reliable` wrappers of the same name on `AZSPlayerCharacter`, mirroring the existing `Server_TakeContainerItem` pattern; the equip/mount-slot widgets call these instead.
- Manifest fully rewritten a fourth time to match — every card's Parent/Bind/Steps rows updated, the old separate "per-item take & Take All" container card merged into `WBP_ZS_ContainerLoot`'s own card (now automatic C++), Foundation table updated with the new shared getters and the RPC-wrapper fix. HTML tag balance verified before republish (same URL).
- **Needs a full `Build.bat` rebuild** — this stacks on top of the accuracy-multiplier and earlier widget-helper passes below; nothing in this whole chain has been compiled yet this session.

**T6/T7/T8 — commit `00d3633` (unchanged by the redesign):**
- **T6 (container loot)**: `AZSContainerActor::Server_TakeItem` (GUID-exact per-item take, dupe-safe by construction), `Server_TakeAllItems`, `Server_AddItemToContainer`, `OnContainerSlotsChanged`, and the client→server RPC wrappers on `AZSPlayerCharacter`. **Open UX question**: should interacting with a container open a real loot screen instead of auto-looting? Not decided.
- **T7 (death/respawn/sleep)**: new `FZSDeathInfo`/`GetLastDeathInfo()` - "cause of death" didn't exist before this. `GetRespawnDelaySeconds()`. `AZSGameState::GetSleepReadyCounts()`. **Open question (OQ-B6-07)**: fuller death-recap screen, not decided.
- **T8 (main menu/pause)**: new `UZSGameInstance` (the project had none before). `HostGame`/`JoinGame`, loading-screen delegates. **Steam invite deliberately not built** (needs `OnlineSubsystemSteam`, an infra decision for you). **Open question (OQ-B1-03)**: solo pause behaviour.

**T1/T2/T5.0 status, unchanged since 2026-08-01 (still current):**
- **T1 (Input-mode switching) — DONE**, solo PT1 passed clean in PIE 2026-08-01 (dev-confirmed). Still open, not blocking: PT1's 2-client half (deliberately deferred as its own session).
- **T2** — C++ done, plus `DA_ZS_UIStyle_Default` and `WBP_ZS_Base` (dev-confirmed created 2026-08-01).
- **T5.0** — Hip→Duffle/weapon-mount data model, compiled and automation-tested as of `de612a5`. **Real content risk still open**: `DA_Bag.uasset`'s `EquipSlot` needs a manual check (not yet done).

**Full click-by-click / per-widget steps** are in **`B1_UI_UX.md`'s "Manual setup steps" section** - every task group (T1-T8) now has its own entry listing every `WBP_ZS_*` widget still to build and every open question flagged inline. A compiled visual checklist (hierarchy diagrams, screen wireframes) of the same list also exists as a private Claude artifact (not part of the repo) - fully re-synced 2026-08-02 for the whole-manifest C++ conversion above: every card's Parent row now names its C++ class, every Bind row says what's already wired, every Steps list is down to Designer-tab hierarchy + Class Defaults (no Graph-tab node-wiring left except a handful of explicit "Create Widget → Open As Modal" call sites), and the Foundation table lists the new shared getters and the RPC-wrapper bug fix.

## B0 exit summary (closed 2026-07-30, practically not 100% formally)

Everything B1 builds UI against is solo-PIE-confirmed: item instances (`FZSItemInstance` refactor), weapon/combat mechanics, camera/aim, `BT_Zombie`, needs simulation. Two real bugs found and fixed this session: zombie bite zone weighting (was always Torso, now a weighted random roll) and `BT_Zombie` freezing after one melee hit (stale `bIsInMeleeRange` Blackboard bool, `AZombieAIController::Tick`, commit `1693884`).

**Two scope decisions moved formal B0 exit items to later phases** (both dev calls, "not a concern right now"):
- 2-client PIE verification → folds into **B1's own exit sweep** instead (debug-console-only feedback made it impractical to judge; far more legible once B1's HUD exists). Detail: `B0_Stabilization.md` Exit criteria, `B1_UI_UX.md` Exit criteria.
- B0-T12 performance baseline → moved to **B8** entirely, not carried as a checkpoint. Reverses the plan's original "profile early" requirement — B8 now captures its own before/after baseline instead of importing an early one. Detail: `B0_Stabilization.md` T12, `B8_Performance.md` Entry/Exit criteria.

**Three items carried forward, not blocking B1** (revisit whenever convenient, none of them gate B1 work):
1. Sections 6-7 (two-tier infection, amputation/blackout) — code complete, still PIE-unverified, dev-deferred since 2026-07-29. May end up easier to verify once B1 gives real UI feedback instead of `ZS.DebugListWounds` log dumps.
2. 5 parked automation-test failures, re-run and re-confirmed 2026-08-01 (`B1T6T7T8_AutomationRun.log`, the fourth clean run in a row) — still exactly these 5, unchanged by anything in B1 so far: `ZS.Combat.DownedZombieAutoRecovery`, `ZS.Combat.ZombieBiteZoneWeightedRoll`, `ZS.Combat.ZombieDeathWhileDownedClearsDownedFlag`, `ZS.Health.AmputationChoreographyEntersBlackout`, `ZS.Inventory.BagStoreAndRetrieve`. None diagnosed further this pass — still open.
3. PT6's full-stage-sweep-A–G has never run as one explicit single pass (pieces separately confirmed).

Full detail on all of the above: `Docs/Beta/B0_Stabilization.md` and `Docs/Beta/B0_ChecklistAndDecisions_2026-07-26.md` (both still current, kept for reference — B0 isn't deleted, just no longer the active phase).

## Next step

**Rebuild first — everything from 2026-08-02 is uncompiled.** Four stacked changes this session, all riding on the same rebuild: the accuracy-multiplier change (`GetAccuracySpreadMultiplier()`), the manifest-widget-logic-into-C++ helper pass (`UZSUserWidgetBase` getters, `GetWoundDisplayCondition`, `GetKeyLabelForHotbarIndex`, `UZSDragDropPayload::Make`), the `UZSBodyConditionIndicatorWidget` trial, and — the big one — the full-manifest widget-class conversion (~23 new files under `UI/`, plus `AZSPlayerCharacter`'s 3 new equip/mount RPC wrappers). Touches `ZSHealthComponent.h/.cpp`, `ZSHealthConfig.h`, `ZSHealthTypes.h`, `ZSUserWidgetBase.h/.cpp`, `ZSPlayerCharacter.h/.cpp`, `ZSDragDropPayload.h/.cpp`, every new `UI/ZS*Widget.h/.cpp` pair, and `Tests/ZSAutomationTests.cpp`. Full `Build.bat`, not Live Coding (header changes + new files under `UI/`, same reasoning as always). Re-run the `ZS.` suite after - confirm both `GetAccuracySpreadMultiplier`/`GetWoundDisplayCondition` tests pass alongside the existing 5 pre-existing failures (still expected, unrelated). All new C++ was hand-verified against engine source/project headers for the two error classes already hit this session (see the trial paragraph above) but is still genuinely uncompiled - a first full rebuild of this much new code may surface something new.

**Once the rebuild is clean, every remaining B1 Widget Blueprint (except `WBP_ZS_Settings`) needs reparenting to its new C++ class, then Designer-tab hierarchy + Class Defaults only — no Graph-tab wiring left anywhere in T3/T5/T6/T7/T8.** The manifest artifact has the exact class name, hierarchy diagram, and steps per card; `B1_UI_UX.md`'s Manual setup steps section has the same per-task-group. Before diving into a screen, check for any open design question flagged on it - several are deliberately left undecided (container-interact behavior, solo pause OQ-B1-03, death-recap scope OQ-B6-07, Steam invite infra, non-weapon Equipment-slot items, no-numeric-health-bar, keyboard-equivalents for every drag target) rather than guessed past.

**One required manual step, separate from widget-building**: set `GameInstanceClass` to `ZSGameInstance` in Project Settings → Maps & Modes (or a BP child of it) - without this, none of T8's host/join/loading-screen code runs at all. Not done yet, deliberately left for you since `Config/DefaultEngine.ini` already has your own uncommitted changes this pass didn't touch.

Two smaller items still open regardless: **check `DA_Bag.uasset`'s `EquipSlot`** for the Hip→Duffle content-risk (not yet done), and PT1's 2-client sweep (deliberately deferred as its own session). Read `Docs/Planning/B1_UIDesignSession_2026-07-30.md` first if picking any screen up cold.

## Known tooling gotchas (worth remembering)

- No PIE-input automation path exists — every manual test in `B0_ChecklistAndDecisions_2026-07-26.md` genuinely needs your hands.
- A live PIE Details panel can show a replicated `TArray`-of-struct component property (e.g. `BodyZones`) as empty/greyed even when the real data is correct — cross-check with a `ZS.DebugList*` console command instead of trusting the panel.
- Live Coding does **not** reliably pick up changes under `Source/ZombieShooter/Tests/`, or any header change (new `UPROPERTY`/`UFUNCTION`) — full `Build.bat` rebuild for both.
- The automation-test command's `-log=name.log` argument didn't take effect in practice 2026-07-30, but did work correctly 2026-08-01 (two separate runs, both produced the named file) — inconsistent, cause unknown. Check the default `Saved/Logs/ZombieShooter.log` too if a named log file appears missing.
- A component added to an actor post-spawn via `NewObject`+`RegisterComponent()` does **not** reliably get `BeginPlay()` called in a synthetic (non-PIE) world — needs to be a real constructor subobject instead, or `DispatchBeginPlay()` called explicitly.

## Other still-open items (lower priority)

Crouch pose bug untouched. Temporary debug instrumentation (`ZS.Debug*` console commands, muzzle-trace debug draw, on-screen hit confirmations) still needs removing once real UI exists — don't remove yet, actively used for testing.
