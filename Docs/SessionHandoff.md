# Session Handoff

> Read this first, every session. This file is rewritten every session, not appended to — it holds only the last completed task and what's needed next. This is the **sole owner of verification status** (compiled? PIE-tested?) — `CLAUDE.md` and `Docs/Phases/P<N>_*.md` describe architecture/checklist state, not status, to avoid the same fact needing edits in three places. Plan of record: `Docs/GameDevPlan.md`. Conventions: `CLAUDE.md`. Full history lives in git commit log, not here.

## Last completed (2026-07-21, same-day follow-up #5)

Dev confirmed the bare-fist punch works end-to-end (see git history for the full attack-input saga this session: hitscan+melee built, missing-mesh Live Coding bug fixed, `IA_Fire`/`IA_Attack` double-trigger fixed, hit-confirmation logging added). Asked to move on to the zombie chase-and-bite test next.

**Pre-flight check via `unreal-mcp` before handing this off** (to avoid the dev burning a PIE cycle on something that couldn't have worked): found a real blocker. `DA_ZS_ZombieConfig_Shambler::BehaviorTree` was still **unassigned** (`None`) - despite `BT_Zombie` being fully wired to `AZombieAIController` last session, the data asset itself never actually pointed at the tree, so `RunBehaviorTree` would have no-op'd and the placed test zombie would never move, perceive, or attack at all. **Fixed directly via `unreal-mcp`** (`ObjectTools.set_properties`, no C++/recompile needed) - `BehaviorTree` now points at `/Game/ZS/Enemy/AI/BT_Zombie`. **This is an in-memory editor change, not yet saved to disk** (`DA_ZS_ZombieConfig_Shambler.uasset` doesn't show as modified in git) - it'll work for PIE right now, but **save it** (select the asset in Content Browser, Ctrl+S, or Save All) before closing the editor or it's lost. Also confirmed via the same check: `ZombieMesh`/`AnimClass` are both still unset on that config (already known - see `P4_Zombies.md`'s AnimBP task) - **the test zombie will be invisible**, not broken. Trust the on-screen/log confirmation below over what you can see.

**Added incoming-damage feedback** (`UZSHealthComponent.cpp`, `.cpp`-only, no header changes - Ctrl+Alt+F11 is safe): the outgoing-damage logging added earlier this session only covered the player's own punch/gunshot - a zombie biting the player had zero feedback of any kind. Added the same temporary `UE_LOG`/on-screen-message pattern to `Server_ApplyDamage` (red "X took Y dmg (WoundType) - HP Z" on every hit) and `Server_RollForInfection` (purple "X INFECTED (Incubating)" if the hidden bite-infection roll hits, a log line either way so a miss is distinguishable from "nothing happened"). All of this is explicitly temporary, same as the earlier round - remove once real hit-reaction VFX/UI exists.

**Not yet tested**: none of this round's fixes have been PIE-verified yet - the BT assignment and the new incoming-damage logging are both untested from here.

## Next step - the chase/bite test itself

1. **Recompile** (`.cpp`-only this round, Ctrl+Alt+F11 is fine).
2. **PIE test**: there's one zombie already placed in `Lvl_ThirdPerson` (`AZombieCharacter_C_UAID_047C164F47BF64F002_1780414535`), now BT-active. It's invisible (no mesh yet) - walk toward its last-known location (or just toward where you last saw it placed) or make noise (sprint, fire a shot) within ~3000 units (`HearingRange`) to get its attention, or get within ~1500 units (`SightRadius`) for it to see you directly.
3. **Watch for**: the zombie's capsule should start moving toward you once it perceives you (you may need `Show Collision` or similar to visually confirm an invisible capsule is moving - or just watch/listen for the melee-hit confirmation once it's close). At ~150 units it should throw a bite (`AttackInterval` 1.5s, `MeleeDamage` 15) - you should see the red "took 15 dmg (Bite) - HP X" on-screen message and a matching Output Log line. Watch for the infection-roll message right after (purple if it hits, silent-but-logged if it misses - `BiteInfectionChance` is 40%, so don't expect it on the first bite necessarily).
4. If the zombie never moves at all: something's still wrong with the BT/perception chain beyond what was checked here (worth reporting back exactly what you saw, or lack thereof, so it can be diagnosed the same way the mesh bug was - via the Output Log, not guessing).
5. Once a bite + infection roll is confirmed: test a treatment action (bandage/disinfect/splint via `Server_UseItem`) and emergency amputation (`AmputateZone`) to close out P3's full loop.
6. **P5's remaining scope** (real `PrimaryHand`/`SecondaryHand`/hotbar loadout, per-weapon melee stats) is documented but not started - read `Docs/Phases/P5_CombatCompletion.md` and `GameDevPlan.md` §7's P5 open questions before picking it up.
7. Carried forward, still open: `BT_Zombie`'s wander branch needs `BTTask_Wander` wired into `BTComposite_Sequence_5` (chase-on-perception doesn't need this, but the zombie won't patrol/discover the player on its own without it); `BP_ZombieAIController`'s fate (delete/repurpose); Zombie AnimBP not created (zombie will stay invisible until then); crouch pose bug untouched; remove all the temporary hit-confirmation logging/on-screen messages (player attack + incoming damage + infection roll) once real impact/hit-reaction feedback is built.
