# Session Handoff

> Read this first, every session. This file is rewritten every session, not appended to — it holds only the last completed task and what's needed next. This is the **sole owner of verification status** (compiled? PIE-tested?) — `CLAUDE.md` and `Docs/Phases/P<N>_*.md` describe architecture/checklist state, not status, to avoid the same fact needing edits in three places. Plan of record: `Docs/GameDevPlan.md`. Conventions: `CLAUDE.md`. Full history lives in git commit log, not here.

## Last completed (2026-07-20, latest session)

**Phase 2 fully confirmed working in PIE this session**: needs decay, sprint/stamina gating, world clock, and 2-client sleep/time-skip aggregation all dev-tested and passing. That work was committed and pushed (commit `c4fc23e`, merged with a remote edit at `366e167`).

**Phase 3 (health/damage/medical) and Phase 4 (zombies) built autonomously this session, per dev instruction to push as far as possible while away — none of it has compiled or been tested at all yet.** Full detail in `CLAUDE.md`'s Architecture section (`Combat/`, `Zombies/` bullets) and `Docs/Phases/P3_HealthDamageMedical.md`/`P4_Zombies.md`'s task checklists. Summary:

- **P3**: `UZSHealthComponent` (4-zone wounds, bleed, Knox-style infection arc, amputation, mobility/attack-speed/reload-speed multipliers - the last two wired into actual movement speed and weapon fire-rate). `AZSPlayerCharacter::TakeDamage` routes all damage in (zone from hit bone, wound type from `DamageEvent.DamageTypeClass`). Death → delayed destroy + `RestartPlayer` respawn-as-new-character. `UZSItemConfig` extended (P2's `bIsConsumable` bool replaced with an `EZSItemUseType` enum: Consumable/Bandage/Disinfectant/Splint) and a unified `Server_UseItem` dispatcher added.
- **P4**: `AZombieCharacter` (flat health, NOT `UZSHealthComponent` - that component is player-specific, see `CLAUDE.md`), `AZombieAIController` (AIPerception sight+hearing wired to `UZSZombieConfig`), `UZSZombieConfig` (speed/health/senses/damage/mesh/BT per type), `UZSNoiseSystem` (wraps `UAISense_Hearing::ReportNoiseEvent`, wired into weapon fire and sprint start). **No Behavior Tree/Blackboard asset exists** - `RunBehaviorTree` is a graceful no-op until one is authored; nothing calls `Server_MeleeAttack`/`SetChasing` yet since there's no BT to call them.

**Partial validation**: `Build.bat` still hit the same Live Coding lock as every session (editor open), but this time UHT actually ran first and processed all 22 new/changed reflected files (every new `UCLASS`/`USTRUCT`/`UENUM`/`UFUNCTION`/`UPROPERTY` across `Combat/` and `Zombies/`) with **zero errors**. That's real signal - UHT is strict about reflection macro correctness - but it does not catch plain C++ errors (mismatched signatures, logic bugs, missing includes). The actual compiler pass has never run on any of this. Every cross-folder `.cpp` include in this batch uses the two proven-safe patterns from earlier this session (`../Folder/X.h` relative, or full `ZombieShooter/Folder/X.h`) rather than bare includes against `PublicIncludePaths`, since that's exactly what broke once before (`ZSInteractableComponent.h` C1083).

**Committed and pushed**: yes, all of this session's work (P2 confirmation fixes + full P3/P4) is on `origin/main` as of this handoff - see the final commit for the exact file list.

## What YOU need to do (nothing below can be done without the editor)

1. **Rebuild first** (Rider, or close the editor + `Build.bat`, or Ctrl+Alt+F11) - this is a big batch, treat all of it as unverified until a real compiler pass succeeds. Fix whatever it reports; I can't iterate on compile errors I can't see.
2. **Create tuning data assets** - nothing in P3/P4 does anything until these exist and get assigned:
   - `UZSHealthConfig` instance (e.g. `DA_ZS_HealthConfig_Default`) → assign to `BP_ZS_PlayerCharacter`'s `HealthComponent`.
   - `UZSZombieConfig` instance (e.g. `DA_ZS_ZombieConfig_Shambler`) → assign to whatever Blueprint child of `AZombieCharacter` you create (see next step - there's no `BP_ZS_Zombie` yet, only the native C++ class).
   - Optionally: `UZSItemConfig` instances with `ItemUseType = Bandage/Disinfectant/Splint` to test the new medical item paths (the existing eat/drink item-use path from P2 still needs its own `DA_ZS_ItemConfig_*` too, per last session's note - still not created as of this handoff).
3. **Create `BP_ZS_Zombie`** (Blueprint child of `AZombieCharacter`) and assign a `UZSZombieConfig`. Place one in the level to test perception/damage - there's no spawner system, so for now this is manual placement.
4. **Source zombie visuals** - `UZSZombieConfig::ZombieMesh`/`AnimClass` are ready to receive a Mixamo/UE-mannequin skeletal mesh + AnimBP; nothing sourced yet.
5. **Author a Behavior Tree + Blackboard** for zombies - this is the biggest missing piece for P4 to actually do anything. `AZombieAIController::ConfigurePerceptionAndBehavior` calls `RunBehaviorTree(Config->BehaviorTree)` the moment one is assigned. `AZombieCharacter::Server_MeleeAttack(AActor* Target)` and `SetChasing(bool)` are the two C++ entry points a BT task should call for the "attack" and "chase" behaviors; perception hookup (`AZombieAIController::HandleTargetPerceptionUpdated`) is currently an empty stub - decide there whether to write a Blackboard key from C++ or handle it entirely in the BT's own perception task.
6. **PIE-test P3 once configs exist**: shoot/melee a player character (a scripted damage source, or just have a second player hit the first with a weapon) and confirm a wound appears, bleeds, and can be bandaged/disinfected/splinted via `AZSPlayerCharacter::UseItem`. Test a Bite specifically (needs `UZSDamageType_Bite` as the damage type on whatever's dealing the test damage) to see the infection arc progress, and test `AmputateZone` stopping it in time.
7. **Crouch bug**: still deferred from prior sessions, untouched. Live-property read of `ACharacter::bIsCrouched` during a real crouch press is still the next diagnostic step whenever picked back up.
8. **Manual cleanup whenever convenient**: `IA_RotateCameraLeft`/`IA_RotateCameraRight` assets + `IMC_ZS_Default` mappings are still orphaned (from the P1 camera-rotation removal) - delete from `/Game/ZS/Input/`.
9. **P0's one open item** (2-client PIE verification of the pre-pivot fire/reload/aim/sprint/crouch replication layer) is still outstanding, separate from any of this session's new-system testing.
10. **Not attempted this session, explicitly deferred**: zombie population/spawner system (zone-based, respawn-into-cleared-zones - more level-design work than a C++ task), performance profiling (nothing to profile without placed zombies), moodle UI for either Needs or Health (UMG, presentation-layer work).
