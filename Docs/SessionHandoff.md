# Session Handoff

> Read this first, every session. Full plan: `Docs/CoreLoopPlan.md`. Project conventions: `CLAUDE.md`. Update this file at the end of every work session, not just phase boundaries.

## Current status (2026-07-12, session 3)

**Phase 0 — done. Phase 1 — done, compiled and PIE-tested clean. Phase 2 — in progress: all pure-C++ milestones (M1/M2/M3/M5 of 9) are code-complete, uncompiled.**

Between session 2 and this session, the dev confirmed (outside any file this project tracks automatically): Phase 1's input-wiring code compiled and PIE-tested clean (move/look/jump/mouse-look all working), the Git LFS $0 spending budget was set, and the Infima Tactical FPS Animations pack was installed successfully (`Content/InfimaGames/TacticalFPSAnimations/`, confirmed to be the Assault Rifle product, asset prefix `AR`).

### Phase 2 plan and key architecture decisions
Full milestone table and the deliberate deviations from Infima's demo (dual FP/TP mesh components, engine-native spring interp, real ammo/reload state, no `BPI_TFA_AnimationState`-equivalent interface, deferred weapon/magazine AnimBPs, etc.) are written into `Docs/CoreLoopPlan.md`'s Phase 2 section — read that before touching any Phase 2 code, it's the plan of record, not this file.

### Done this session
- **M1** — `Source/ZombieShooter/Player/ZSCharacterTypes.h` (`EZSCameraPerspective`, `EZSStance`, `FZSSpringConfig`), `Source/ZombieShooter/Weapons/ZSWeaponTypes.h` (`EZSFireMode`, `EZSGripAttachment`), `Source/ZombieShooter/Weapons/ZSWeaponConfig.h/.cpp` (`UZSWeaponConfig : UPrimaryDataAsset`, ~130 fields covering every group from the Infima guide's `BP_TFA_BaseConfig` plus this project's own real ammo/fire-mode/recoil fields).
- **M2** — `AZSWeapon` (`Source/ZombieShooter/Weapons/ZSWeapon.h/.cpp`) and its supporting actors: `AZSMagazine`, `AZSPhysicsObject` (+ `AZSPhysicsMagazine`/`AZSPhysicsCasing` children), `AZSLaserAttachment`. All null-safe against an unpopulated config.
- **M3** — `AZSPlayerCharacter` Phase 2 additions: `FirstPersonMesh`/`FirstPersonCamera` components alongside the existing TP `CameraBoom`/`FollowCamera` pair, 4-perspective camera switching (`ToggleCameraPerspective`), spring-based ADS/Recoil/Crouch procedural offsets (`UKismetMathLibrary::VectorSpringInterp`/`QuaternionSpringInterp`), action-state flags (`bIsBusy`/`bIsAimingBlocked`/`bIsAiming`/`bIsSprinting`) with single-mutation-point setters (`SetBusy`/`SetAimingBlocked`, ready for Phase 3 to prefix `Server_`/wrap `OnRep_` without touching call sites), and the combat/movement function set (Fire/Reload/Inspect/MagCheck/FireMode/Grip/Aim/Sprint/Crouch). `EquipWeapon()` spawns an `AZSWeapon` from `StartingWeaponConfig` on `BeginPlay` if set (nothing sets it yet - that's M6).
- **M5** — `UZSAnimInstanceBase` + `UZSFirstPersonAnimInstance` (`Source/ZombieShooter/Player/Animation/`), native only, no AnimGraph content yet. All 7 notify/notify-state classes (`Source/ZombieShooter/Weapons/Notifies/`): `AN_ZS_DropMagazine`, `AN_ZS_EjectCasing`, `AN_ZS_UnlockActions`, `ANS_ZS_BlockADS`, `ANS_ZS_HideMainMag`, `ANS_ZS_ShowReserveMag`, `ANS_ZS_LeftHandGrip`.
- **M4, M6-M9 not started** — M4 (new `IA_*` input assets) and M6+ (content population, AnimGraphs) all need live editor/MCP work, deliberately deferred until this pure-C++ block gets a real compile pass (see below).

**~30 files touched across M1/M2/M3/M5. None of it has been compiled yet** — same computer-use limitation as session 2 (can't attach to the Unreal Editor window), so this is entirely unverified beyond careful manual read-throughs and a couple of targeted typo greps.

### Not yet done (the very next steps, in order)
1. **You:** press **Ctrl+Alt+F11** for a Live Coding pass (or a clean `Build.bat` run if you'd rather start from a cold cache, given the size of this change). Report back any compile errors - there's a real chance of at least one on a change this size with zero compiler feedback during writing.
2. Once it compiles: **M4** (create `IA_Fire`/`IA_Aim`/`IA_Reload`/`IA_Crouch`/`IA_Sprint`/`IA_ToggleView`/`IA_FireModeSwitch`/`IA_Inspect`/`IA_MagCheck`/`IA_SwitchGrip` under `Content/ZS/Input/`, add mappings to `IMC_ZS_Default`) and **M6** (populate `DA_ZS_WeaponConfig_AssaultRifle` by cross-referencing Infima's own `DA_TFA_AssaultRifle` instance via MCP, create a thin `BP_ZS_PlayerCharacter` to set `StartingWeaponConfig`, repoint `AZSGameMode::DefaultPawnClass`) are both live-editor/MCP milestones, natural to do together in the next session.
3. **M7-M9** (real mesh assembly + all 4 camera perspectives, the FP/TP AnimGraphs, notify placement + multi-weapon regression check) come after M4/M6. **M8 (AnimGraphs) has an unresolved MCP-capability risk** — `editor_toolset.toolsets.blueprint.BlueprintTools` exposes a generic `EdGraph` DSL read/write, but whether it understands anim-specific nodes (state machines, `BlendListByBool`, `LayeredBoneBlend`, FABRIK) is unconfirmed. De-risk this **before** M8 by doing a no-op DSL round-trip on an AnimGraph early - ideally right after M4/M6's compile is confirmed clean, so there's a fallback plan (manual, at-the-keyboard AnimGraph authoring) well before M8 is actually blocking.

## Open decisions not yet made
- Whether `Content/ThirdPerson/` (stock demo level + Blueprints) and `Content/Characters/Mannequins/` are kept as a placeholder test level / potential Phase 6 TP-locomotion source, or removed — currently **kept, untouched**.
- `ConstructorHelpers::FObjectFinder` vs. a thin Blueprint child purely for asset-default wiring: `AZSPlayerCharacter` now has 14 `ConstructorHelpers::FObjectFinder` calls in its constructor (4 from Phase 1, 10 new this session) and will pick up more once `StartingWeaponConfig` matters in M6. Session 2 flagged this as worth reconsidering once the list kept growing - it has. Genuinely worth a decision before M6: either keep going (simple, consistent, no Blueprint needed), or introduce the thin `BP_ZS_PlayerCharacter` M6 already needs anyway for `StartingWeaponConfig` and migrate the `ConstructorHelpers` calls onto it too, matching what the *original* stock template actually shipped (`BP_ThirdPersonCharacter`). Not changed unilaterally - flagging for the dev to weigh in before M6.

## Known risk flagged, partially addressed
Session 2's compile-verification win (Phase 0/1 renames confirmed compiling) does **not** extend to this session's ~30 new/changed files - zero compiler feedback was available while writing them (same computer-use limitation, see below). Treat everything under "Done this session" as "carefully written but unverified" until the Ctrl+Alt+F11 pass in "Not yet done" #1 comes back clean.

## Session note (2026-07-12, session 1)
Dev pointed out a disconnect between file-level Phase 0 work (done from a ShooterGame-rooted session) and the actual Rider/editor state. Confirmed a session's MCP access and CLAUDE.md auto-load are tied to its working-directory root. Recommended starting future sessions rooted at ZombieShooter directly — **acted on for session 2**, confirmed working.

## Session note (2026-07-12, session 2)
Computer-use (desktop automation) cannot target the Unreal Editor window — `request_access(["Unreal Editor"])` returned `notInstalled`, since the tool only resolves Start-Menu-style installed-app names and Unreal Editor isn't launched that way here (it's opened via Rider / directly). This means **Live Coding compiles and PIE playtesting cannot be driven by Claude Code in this environment** — they're a manual, at-the-keyboard step every session, not something to keep re-attempting via computer-use. **Confirmed still true in session 3** — no new attempt was made to work around it, this is treated as a standing environment constraint now, not something to re-litigate each session.

## Session note (2026-07-12, session 3)
Phase 2 planning went through a full explore-plan-review cycle (an Explore agent read all 15 files of `Docs/Infima Pack - Official Implementation Guide/` plus the actual installed content structure; a Plan agent turned that into the milestone breakdown + architecture decisions now in `CoreLoopPlan.md`). Implementation then proceeded through all four pure-C++ milestones back-to-back without stopping for a compile check between them, specifically because there was no way to compile-check mid-stream (same computer-use limitation as session 2) — stopping after each milestone would have cost review time without buying any actual verification. The tradeoff: a single large, unverified change instead of several small verified ones. This was a deliberate call given the tooling constraint, not an oversight - flagging it so the next session doesn't assume incremental verification happened when it didn't.
