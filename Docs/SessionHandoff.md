# Session Handoff

> Read this first, every session. This file is rewritten every session, not appended to — it holds only the last completed task and what's needed next. This is the **sole owner of verification status** (compiled? PIE-tested?) — `CLAUDE.md` and `Docs/Phases/P<N>_*.md` describe architecture/checklist state, not status, to avoid the same fact needing edits in three places. Plan of record: `Docs/GameDevPlan.md`. Conventions: `CLAUDE.md`. Full history lives in git commit log, not here.

## Last completed (2026-07-21, same-day follow-up)

Dev asked for two things after the attack-path round: (1) a real, researched **loadout system** plan (player starts unequipped, switches weapons/items) plus a **unified single-button attack** that changes behavior per equipped weapon — scoped as a new phase, not built yet; (2) the placeholder `IA_Melee` renamed to `IA_Attack` (dev is creating that input action now and mapping it to left-click) so today's melee testing isn't blocked on the rename.

**Planning (added to `Docs/GameDevPlan.md`, not built):**
- **P5 expanded and retitled**: "Combat completion" → **"Loadout & Unified Combat"** (`Docs/Phases/P5_CombatCompletion.md` updated to match, filename kept). Researched against the two closest proven references for a real-time, no-pause-and-plan survival game (Decision 1 rules out a menu/ring-select system): PZ's primary/secondary-hand equip model and DayZ's hand-slots + hotbar. Recommended design: `PrimaryHand`/`SecondaryHand` equip slots (two-handed items occupy both; this is also the prerequisite the already-recorded amputation backlog item needs — "arm amputation restricts to one-handed weapons" is meaningless without a real one/two-handed slot model to enforce it against), empty `PrimaryHand` = bare-fist unarmed default, a real-time hotbar referencing into P6's inventory for instant re-equip, equip/holster choreographed through the existing notify/`bIsBusy` system like reload.
- **Unified attack input, same section**: `UZSWeaponConfig` grows an `EZSAttackType` (Unarmed/Melee/Ranged) field so melee weapons go through the same config pipeline as guns (no parallel data-asset hierarchy); a future `Server_Attack` dispatches on whatever's in `PrimaryHand` — null → bare-fist, `Melee` → weapon-stat swing, `Ranged` → today's hitscan logic, with `HandleFireStarted`'s auto-fire timer moving under this dispatch too (this is what retires `IA_Fire` as a separate action).
- **P6 cross-referenced, not changed in scope**: added a note that `PrimaryHand`/`SecondaryHand`/hotbar are P5's, not P6's — P6 still owns the actual `UZSInventoryComponent`/weight/containers/loot tables those slots reference into.
- **New open questions in §7** (P5: hotbar key scheme, whether `SecondaryHand` is ever independently active, equip-timing model) and a **new cross-cutting open question**: the dev's left-click dual-purpose note (menu-select vs. attack/interact, same physical click) — flagged as **not built, no menu exists yet to gate against** (P1's interaction is `E`/`IA_Interact`, not a click). Documented mechanism for when it's needed: an `IMC_ZS_UI` mapping context added with higher priority than `IMC_ZS_Default` while a menu has focus (Enhanced Input's normal context-swap pattern), not a per-click `bIsMenuOpen` branch inside the attack handler.

**Code (the "make changes needed for testing" part, done now rather than deferred to P5):**
- `MeleeAction` → `AttackAction` (constructor finder now looks for `/Game/ZS/Input/IA_Attack`), `CanMelee()` → `CanAttack()`, `HandleMeleeAttack()` → `HandleAttack()`.
- Tunables renamed with an `Unarmed` prefix (`MeleeDamage`→`UnarmedMeleeDamage`, `MeleeRange`→`UnarmedMeleeRange`, `MeleeAttackInterval`→`UnarmedMeleeAttackInterval`, `MeleeDamageTypeClass`→`UnarmedMeleeDamageTypeClass`, `MeleeMontage`→`UnarmedMeleeMontage`) — done now so there's no second rename pass once P5 adds real per-weapon melee stats; these are explicitly the bare-fist fallback going forward, not a temporary name.
- `Server_MeleeAttack` itself is unchanged in behavior — still unconditionally bare-fist melee, no dispatch. That dispatch is P5 scope, not built this round.
- `AZombieCharacter`/`UZSZombieConfig`'s own `MeleeDamage`/`MeleeRange` fields are unrelated (zombie attack stats) and were **not** touched.

**Compile status: still unverified** — same as last round, the editor was open with Live Coding active. This round's changes are a pure rename (no new UPROPERTY/UFUNCTION signatures beyond what compiled last round, just renamed), so the risk profile is lower, but still needs a real recompile before PIE testing.

## Next step

1. **Create `IA_Attack`** (dev is doing this) and map to left-click in `IMC_ZS_Default`. C++ already points at `/Game/ZS/Input/IA_Attack` — no further rename needed once the asset exists.
2. **Recompile** — editor's Compile button, or close editor + `Build.bat` if that doesn't take.
3. **PIE test**: left-click should now throw a bare-fist punch at the nearest zombie in front of the player (same `Server_MeleeAttack` logic as before, just reachable via the new input). `IA_Fire` (if separately mapped) still fires guns independently — no conflict expected since they're different bound actions, but worth confirming both work if both end up mapped to left-click during this transitional period.
4. **P5 is documented, not started** — when picked up: build the `PrimaryHand`/`SecondaryHand` loadout slots, generalize `UZSWeaponConfig` with `EZSAttackType`, and build the real `Server_Attack` dispatch. Read `Docs/Phases/P5_CombatCompletion.md` and `GameDevPlan.md` §7's P5 open questions first — there are real unresolved calls (hotbar key scheme, whether `SecondaryHand` can be independently active) worth the dev's input before building.
5. Once damage/attack input is confirmed working: full end-to-end AI+P3 test (wander → hear/see player → chase → melee/bite → P3 wound/infection → treatment/amputation).
6. Carried forward, still open: `BT_Zombie`'s wander branch needs `BTTask_Wander` wired into `BTComposite_Sequence_5`; `BP_ZombieAIController`'s fate (delete/repurpose); Zombie AnimBP not created; crouch pose bug untouched.
